{
  "name": "allay",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspace server && (cd client && npx tsc --noEmit)"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
